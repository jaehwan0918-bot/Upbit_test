const db=require("../lib/db");
module.exports=async function handler(req,res){if(req.method!=="GET")return res.status(405).json({error:"GET only"});const deviceId=String(req.query.deviceId||"");if(!/^[a-zA-Z0-9_-]{8,100}$/.test(deviceId))return res.status(400).json({error:"Invalid deviceId"});try{const rows=await db.select("signal_events",{select:"id,created_at,market,tf,score,quality,adx,vol_ratio,regime,price,push_sent",device_id:`eq.${deviceId}`,order:"created_at.desc",limit:"30"});return res.status(200).json({events:rows||[]})}catch(e){return res.status(e.setupRequired?503:500).json({error:e.message,setupRequired:Boolean(e.setupRequired)})}};
